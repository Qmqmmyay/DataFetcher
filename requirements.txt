# VNTrading DataFetcher Requirements
# Generated for easy installation on new computers

vnstock>=0.2.9
pandas>=2.0.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
requests>=2.31.0
tqdm>=4.60.0
tenacity>=8.0.0

# Optional but recommended
numpy>=1.24.0
python-dateutil>=2.8.0

# Note: sqlite3 is built into Python
# Note: vnstock_data, vnstock_ta, vnii are offline packages (578MB total)
# These must be preserved from the original virtual environment
